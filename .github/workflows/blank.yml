name: Zap-scanning

on:
  push:
    paths:
    - 'postman-collection/**'

jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2
    - name : checking for existing container
      run : echo "Hello"
           
