name: Static Testing

on:
  push:
    branches:
      - master
  pull_request:
    branches:
       - master
jobs:
  checkout-repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Print File
        run: |
            ls
            pwd

